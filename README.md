# h264dump

Utility for printing information about h.264 NAL units and syntax elements. 

```
Usage: h264dump [options] <filename>
Options:
  -h, --help:    Show this help message and exit
  -v, --version: Show the version and exit
```
